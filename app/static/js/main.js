// TrueHair AI MVP Main JavaScript

document.addEventListener('DOMContentLoaded', () => {
    // Page-specific interactive logic is handled by inline scripts
    // in each template (e.g. style_studio.html).
});